MONGO_URL=mongodb://localhost:27017
DB_NAME=aquasuino
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=
CORS_ORIGINS=http://localhost:3000
# Local HTTP only. Use true and HTTPS in production.
COOKIE_SECURE=false
# Optional: required only for uploading water-meter photos.
EMERGENT_LLM_KEY=

# Public address used in recovery links; use HTTPS outside local development.
PUBLIC_APP_URL=http://127.0.0.1:8765
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_FROM=
SMTP_USERNAME=
SMTP_PASSWORD=
